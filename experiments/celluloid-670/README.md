This example is to reproduce the memory leak inside Celluloid 0.17.0,
which is described in [this issue](https://github.com/celluloid/celluloid/issues/670).

## Usage

```
bundle install
bundle exec ruby main.rb
```
